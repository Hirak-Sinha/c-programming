#include<stdio.h>
int main(){
float a,b;
float product;
printf("ennter the first number:");
scanf("%f",&a);
printf("ennter the second number:");
scanf("%f",&b);
product=a*b;
printf("product is:%f",product);
return 0;
}
